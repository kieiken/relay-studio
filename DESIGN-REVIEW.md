# UI・UX確認記録 — 2026-09-22

実レビューはClaude Codeの契約ログインを使い、`--model claude-fable-5` を明示。APIキー環境変数を除外し、モデルの自動フォールバックは指定していません。応答のmodelUsageで `claude-fable-5` を確認しました。Claudeの出力に含まれるlist-price costは参考計算であり、課金APIを設定して呼び出したものではありません。

## 第１回：設計

Claude Fableの提案：自動／公式画面の違いをカードで常時表示、３カラムから段階的な編集へ、未接続の見える化、端末稼働条件、結果不明専用の照合フロー。

Codexの判断と反映：共通原稿＋投稿先の２カラムにしてプレビューをタブへ。個別原稿を保持。全媒体チェックを既定にし、未接続・未入力の選択先は黙って外さず登録を停止。Fableが提案した「未接続先を除外する確認」より、利用者自身が明示的にチェックを外す方式を採用した。期限超過の停止、実公開時刻、48時間観測／現在累計、未取得と0の区別を実装。

## 第２回：実画面とapp.js

Claude Fableはデスクトップの投稿作成・初期設定画面、モバイル用スクリーンショットと実装をReadツールで確認。「ローカルアルファとして妥当」と評価し、４項目を指摘した。

1. 共通画像があるとFacebook／Threadsが登録できない。媒体単位で画像だけ外す明示操作を追加し、本文をコピーした個別原稿に切り替える。画像の無断省略はしない。
2. 固定アクションバーの重なり。アクションバーを通常のレイアウトへ移し、入力欄やカードを覆わないよう変更。
3. 個別編集への文脈移動。切替時に編集見出しへスクロール・フォーカス移動し、対象パネルを短く強調。動きを減らす設定も考慮。
4. 登録エラーの総称表示。媒体別・理由別のリスト、配信時刻欄のエラー、aria-invalidを追加。

初回のモバイル画像は画面切替直後の取得競合で反響画面が写っていたため、投稿作成見出しの描画完了を待って撮影するようブラウザ検証を修正した。

## 検証の範囲

Nodeの自動テストと、独立した一時データ／API実通信なしのChromeで初期設定から原稿作成・登録・結果記録・反響記録まで確認。既存運用の投稿やキーは未使用。実アカウント公開、キー変更、購入、販売公開は実施していない。

本稿は設計と検証の記録であり、媒体規約や法的要件の確認書ではない。Fableの一般論をそのまま製品仕様の事実として採用していない。

## 0.1.1：日本語／台湾向け繁體中文

2026-09-22、UIの日本語と繁體中文（台灣）の切り替えを追加。画面上部に言語メニューを置き、ブラウザに選択を保存する。原稿・キー・アカウント名・URL・タイムゾーンは翻訳しない。UIの固定文言とテンプレートの固定部分だけを翻訳し、差し込まれる利用者データは保持する。未保存の設定入力も言語変更時に保持し、キーはブラウザ保存領域へ書き込まない。

Claude Fable（claude-fable-5）の実レビューで台湾表現を確認。「登錄」を排程に合う「排定」へ、共通原稿を「共用內容」、接続とリンクの用語を「連線／連結」へ整理。保存アクションと成効の收藏数も分けた。

レビューで懸念されたplaceholderとaria-labelの漏れは、静的HTML部分をDOM生成前に翻訳する実装のため該当しなかった。ブラウザテストで実際のplaceholder・改行・原稿モードのaria-labelを確認した。辞書漏れの日本語が残る場合を検出する記録も追加し、初期設定・作成・配信・反響・確認ダイアログを巡回して漏れがないことを検証した。

検証：Nodeテスト16件合格。既存ブラウザ操作検証に加え、日→繁→日、再読込時の維持、未保存の接続情報保持、キーがlocalStorageへ入らないこと、共通／個別原稿不変、予定のUTC時刻不変、モバイル横はみ出しなしを確認。レビューとテストで実アカウントの認証・公開操作は行っていない。

## 0.1.3：一度の操作とAIインストール

2026-09-22、契約済みClaude Codeの `claude-fable-5` に製品UI・インストール仕様の独立レビューを依頼しました。実装ファイルを見る前の設計レビューです。原稿作成・レビューは依頼していません。

採用：手動媒体の常時表示、API受付と実公開の分離、未接続の選択先を黙って除外しない、アルファ表示、インストール前のファイル検証、再実行と更新時のデータ保持、秘密情報をAIに渡さない案内、投稿を伴わない隔離診断。通常は日時指定なしの主ボタン一回、予約時だけ確認ダイアログとしました。

この時点では追加の実装レビューは未実施でした。0.1.4でインストール関連５ファイルの実レビューを実施しました（次節）。Node同梱、汎用Chrome拡張、全６媒体の無人実行は未完成です。


## 0.1.4：インストール実装のレビューとGitHub配布

Claude FableはINSTALL_WITH_AI.md、install.mjs、bundle.mjs、diagnose.mjs、start.commandの実内容を独立レビューし、次の４点を指摘しました。Codexが実装を照合して修正しました。修正後の再レビューは依頼していません。

1. ライブラリ欠落時のnpm ci案内と配布マニフェストの整合性。Codexの確認では既存ZIPにnode_modules/.package-lock.jsonも含まれるため、「npm ciで必ず余剰ファイルになる」というClaudeの断定は当たりません。ただし配布物をその場で作り直す案内は避け、配布ZIPでは正規ZIPを再取得して新しい空の設置先へ導入する案内に変更。開発用ソースだけnpm ciを案内します。
2. 同一版でも起動中に停止を要求する処理順。内容照合が一致したら、停止チェックより先にalready-installedを返すよう修正。実際の更新では引き続き停止が必要です。
3. macOSの大小文字による保存先の同一性。既存祖先のrealpathとNFC・大小文字の正規化で比較し、データ・ソース・ホームとの重なりを拒否します。大小文字を区別するボリュームでも安全側で扱います。
4. 起動待ちの堅牢性。待受応答に２秒の制限を設定し、製品名・バージョン・設置場所の識別子を確認。待機ループにも期限を設け、古いNodeのエラーでも利用者が理由を読めるようにしました。

同一版を起動中に再導入しても変更しないこと、別版への更新は止めること、大小文字だけ異なる設置先がデータ領域を侵さないことを自動テストで検証します。秘密情報・実アカウント・実投稿を使わず、配布ZIPの展開から設置・診断まで確認する方針です。
# 0.2.0 拡張レビュー（2026-09-22）

契約済みClaude Codeの `claude-fable-5` へ公開用実装を提示し、独立した実出力を取得しました。二重送信防止のリース・最終操作記録・結果不明時の再送禁止を評価しています。

Xの絵文字・リンク照合、公開URL取得、拡張停止時の待機滞留、同一画面への遷移表示の指摘を修正しました。反響には明示的な現在値取得を追加し、早期取得で48時間後の取得予定を消費しないことをテストしています。

Codexは、送信完了時刻をRedNoteの実公開日時へ代用する提案を採用しませんでした。実日時未確認は未確定として扱います。原因未修正の無条件再試行や、選択した媒体の自動除外も採用しません。共有端末の別利用者に対する認証隔離は備えていないため、信頼できる単一利用者の端末向けであることを明記しました。

このレビューは静的評価です。新しい拡張経由の実投稿・削除の成功証拠は別途必要であり、静的評価だけを完成判定には使いません。

---
