# Relay Studio を Codex / Claude でインストールする

対象：Relay Studio 0.2.0 alpha、macOS、Node.js 22以降。

GitHubから取得する場合は [Releases](https://github.com/kieiken/relay-studio/releases) の実行用ZIPと対応する`.sha256`を使用してください。「Code → Download ZIP」や「Source code」は依存ライブラリと配布マニフェストを含まない開発用です。

ZIPを展開し、**このファイルがあるフォルダをCodexまたはClaude Codeで開いて**、次を依頼してください。ファイルだけをWebチャットへ添付しても、そのチャットからパソコンのファイルを操作できなければインストールはできません。

> このフォルダの INSTALL_WITH_AI.md を読み、Relay Studioをインストールしてください。配布ファイルの検証、設置、投稿をしない起動診断まで実施し、初期設定画面を開いてください。既存データとキーは保持してください。

通常は上の依頼だけで進みます。SNSのログイン、アカウントID・キーの入力は、必要なときに利用者が初期設定画面で行います。キーをチャットへ貼り付ける必要はありません。AIの契約・利用枠と、ローカルファイル／ターミナルを操作できる環境が必要です。

この版は、Facebook・Threads・Instagramの公式API投稿と、X・note・RedNoteのChrome拡張連携を実装しています。拡張は extension フォルダに同梱しています。実機の一貫動作は検証中です。既存のHDSL専用拡張をこの製品の拡張として導入しないでください。インストール成功は全６媒体の無人投稿を実証したという意味ではありません。

## Codex / Claude Code が実行する手順

この文書に記載したインストールと診断だけを実施してください。掲載原稿の生成・投稿・削除・接続テスト・外部送信は、このインストール依頼には含まれません。

1. **環境と配布物を確認する。** このファイル、`scripts/install.mjs`、`scripts/bundle.mjs`、`scripts/diagnose.mjs`、`start.command`を読んでから実行してください。`uname -s` が `Darwin`、`node --version` が22以降であることを確認します。Nodeがない／古い場合は、その原因を伝えて止めます。既存Nodeを削除・置換せず、利用者が別途依頼した場合だけ公式Node.jsの導入を案内します。sudo、API課金、購入は行いません。
2. **ZIPの同一性とファイルを照合する。** ZIPと隣の`.sha256`があれば、そのフォルダで `shasum -a 256 -c Relay-Studio-0.2.0-alpha.zip.sha256` を実行します。配布フォルダで `node scripts/install.mjs --check` を実行し、`status: verified` を確認します。マニフェストの欠落、ファイルの増減・ハッシュ不一致では止めて正規のZIPを取り直します。マニフェストを書き直して検証を通さないでください。Finderが作る.DS_Storeだけは検証・コピーの対象外です。同梱のハッシュは破損検知用であり、配布元の署名を代替しません。
3. **本体を設置する。** 同じフォルダで `node scripts/install.mjs --install` を実行します。既定先は `~/Applications/Relay Studio`。別の場所を利用者が指定した場合は `--destination "/絶対パス/Relay Studio"` を追加します。シェルではパスを引用符で囲みます。インストール先への書込み許可を実行環境から求められたときだけ、その許可を利用者へ説明してください。
4. **再実行・更新を扱う。** 同一の正常な本体なら `already-installed` となり、変更しません。更新では旧本体を隣の `Relay Studio.backup-日時` に退避します。既存の別フォルダや改変済み本体は上書きしません。旧版にマニフェストがない場合も無理に削除せず、別の空の設置先を使います。起動中の更新は止め、利用者の予約に影響しないよう停止のタイミングを確認します。バックアップを自動削除しません。
5. **投稿しない診断を実行する。** 設置先で `node scripts/diagnose.mjs` を実行し、`DIAGNOSIS_OK` を確認します。この診断は空の一時データ・一時ポート・ワーカー無効で起動し、終了時に一時データを削除します。既存SNSアカウント・キー・予約・実投稿を使いません。
6. **初期設定画面を開く。** 診断成功後、`~/Library/Application Support/Relay Studio/studio.json` が存在しない新規環境では、設置先の `start.command` を `open` で開きます。既存データがある環境では、通常起動すると待機中の投稿が実行され得るため、自動では再開せず、診断成功と起動ファイルの場所を案内します。既に8792番ポートで別のRelay Studioが動いている場合は、その画面を新しい版の起動成功と扱わず、停止・切替が必要と伝えます。新規起動後は `http://127.0.0.1:8792/` の初期設定と日本語／繁體中文（台灣）の切替が表示されることを確認します。ターミナルは開いたまま使う必要があります。
7. **結果を簡潔に伝える。** バージョン・設置先・診断結果・画面を開けたか・未完了事項を報告します。アカウント設定がまだなら、本人による入力が残っていることを明記します。実機接続や投稿まで検証したとは報告しないでください。

## Chrome拡張の設置

`extension/manifest.json` と `extension/README.md` を読んで権限を確認してください。設置先の `extension` フォルダを、Chromeの `chrome://extensions` →「デベロッパーモード」→「パッケージ化されていない拡張機能を読み込む」で選択します。既存の拡張は削除しません。操作環境が導入直前の確認を要求する場合は、理由と権限を利用者へ説明します。拡張管理画面が操作ツールで禁止されている場合は迂回せず、この操作だけ利用者へ依頼します。

読み込み後、Chromeで本体の設定画面を開き「拡張機能を接続する」を押します。SNSへのログインとアカウント照合は利用者の設定時に行い、インストールだけで投稿しません。

## 保持するものと禁止する操作

- 原稿・接続先情報・予約・実績は `~/Library/Application Support/Relay Studio` に残します。設置スクリプトはこのデータを変更しません。
- アクセストークンはmacOSキーチェーンに残します。インストール時に読み出したり、AIへ表示したり、別アプリのキーをコピーしません。
- ChromeのCookie・ログインセッション・プロファイルをコピーしません。Chromeのセキュリティ設定を変更しません。
- 旧HDSLの拡張・データ・自動運転設定を持ち込みません。LaunchAgent・ログイン項目・定期実行は登録しません。
- 失敗した場合は原因を特定し、未修正の同じ操作を繰り返しません。ユーザーのファイル・キー・データを消して復旧しません。

## 使用 Codex / Claude Code 安裝（繁體中文・台灣）

解壓縮ZIP後，在可操作本機檔案與終端機的Codex或Claude Code中開啟這個資料夾，再輸入：

> 請閱讀這個資料夾的 INSTALL_WITH_AI.md，安裝 Relay Studio。請完成套件驗證、安裝及不會發文的啟動診斷，並開啟初始設定畫面。保留既有資料與金鑰。

適用於macOS與Node.js 22以上。AI依上方步驟驗證SHA-256與所有套件檔案，安裝至 `~/Applications/Relay Studio`，再執行隔離診斷。重複安裝相同版本不會覆寫資料，更新會保留舊程式備份。若缺少Node、檔案遭更動、程式正在執行或權限不足，會說明原因。請勿將金鑰貼進聊天；由您在初始設定畫面輸入。既有資料存在時，AI不會自行恢復可能發文的背景工作。

目前Facebook、Threads、Instagram可透過官方API發文；X、note、RedNote 透過同梱的 Chrome 擴充功能操作官方頁面，實機驗證仍在進行。請在 chrome://extensions 載入安裝資料夾的 extension；若 AI 工具不允許操作擴充功能管理頁面，這一步須由使用者完成。此版未包含 Node 執行環境。AI 安裝完成並不代表六個平台的全自動發文已通過實機驗證。
