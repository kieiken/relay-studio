# Relay Studio 0.1.4 alpha

Codex／Claude Codeに `INSTALL_WITH_AI.md` を読ませて、配布物の検証・インストール・投稿を伴わない起動診断を進められるmacOS向けアルファ版です。

## ダウンロードと導入

GitHub Releasesの `Relay-Studio-0.1.4-alpha.zip` と対応する `.sha256` をダウンロードしてください。GitHubが自動生成する「Source code」は開発用で、実行用ライブラリと配布マニフェストを含みません。

ZIPを展開し、CodexまたはClaude Codeでそのフォルダを開き、「INSTALL_WITH_AI.mdを読み、インストールしてください」と依頼します。macOSとNode.js 22以降が必要です。SNSのキーは本人が初期設定画面に入力してください。

## 含まれる機能

- 日本語／繁體中文（台灣）のUI切替。
- 全６媒体を初期選択。共通原稿と媒体別原稿を切替可能。
- 通常は主ボタン一回で保存・検証・受付。日時を指定すると予約確認へ。
- Facebookページ・Threadsのテキスト、Instagramの画像を公式APIで配信。
- X・note・RedNoteの原稿準備と公式画面への案内。公開操作は利用者が実施。
- 公開記録、媒体別の反響、48時間観測と現在累計・未取得と0の区別。
- インストール時の整合性検証、更新時の旧版本体退避、既存データ保持。

## 現在の制限

汎用Chrome拡張、全６媒体の無人投稿、Node.jsの同梱、署名済みアプリ、画像・動画のホスティングは含みません。ローカル予約の実行には端末・アプリの起動とネット接続が必要です。利用者ごとのAPI権限とトークン設定が必要です。

## 繁體中文（台灣）

請下載Releases中的執行版ZIP與SHA-256檔案，解壓縮後，在Codex或Claude Code中開啟資料夾，請AI閱讀 `INSTALL_WITH_AI.md` 並安裝。需macOS與Node.js 22以上。

Facebook、Threads、Instagram可透過官方API發布；X、note、RedNote仍需在官方介面完成發布。此版本尚未包含通用Chrome擴充功能或Node執行環境。

## レビューと検証

Claude Fableによるインストール関連５ファイルの独立レビューを実施し、再インストールの判定順、保存先の大小文字、復旧案内、起動待ちを改善しました。24件の自動テストと配布ZIPからの隔離起動診断で確認しています。キーや個人の投稿データ・レビュー生ログは公開物に含めません。
# 0.2.0 alpha 開発版 — 実機検証中

- X・note・RedNote向けChrome拡張を同梱し、本体の一つの投稿ボタンから登録。
- 本人照合、画像ファイル、拡張接続画面、日本語・台湾繁体字を追加。
- 最終公開前の照合、二重操作防止、再起動・失効時の自動再送禁止。
- 反響の現在値取得と48時間観測を分離。

新拡張の実機投稿から削除までの検証は未完了です。0.1.4の手動テストを新拡張の成功実績とは扱いません。

---
